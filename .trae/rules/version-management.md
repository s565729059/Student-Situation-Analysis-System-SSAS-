# 版本管理

## 更新流程
1. 本地修改不更新版本号
2. 用户说"推送"时：版本号+0.0.1，同步4个HTML版本号，更新index.html日志，git push
3. 大版本更新需用户明确说"大版本更新"

## 推送规则
- 仅用户说"推送"时才推送，不主动推送

## 版本号同步位置（缺一不可）
- 4个HTML的`版本号：vX.X.X`文本
- index.html的`CURRENT_VERSION`常量
- HTML中`?v=X.X.X`资源版本号
- HTML中`APP_VERSION`缓存常量

## 更新日志风格
文字要可爱，AI换代表述改为"小睿同学智能升级"
